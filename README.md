# chrome-sync-engine

Sync data across devices in Chrome extensions.

## Installation

```bash
npm install chrome-sync-engine
```

## Usage

```javascript
import { SyncEngine } from 'chrome-sync-engine';

const sync = new SyncEngine();
await sync.sync(data);
```

## License

MIT
